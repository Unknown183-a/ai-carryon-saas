Owned by: **Phase 7 — async-workers (voice_worker.py's TTS call)**.

`client.py` — thin httpx wrapper over ElevenLabs' text-to-speech API,
same pattern as `integrations/gemini/` and `integrations/groq/`.

See `../../../phases/phase-07-async-workers/PHASE.md`.
