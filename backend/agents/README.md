Owned by: **Phase 4 — langgraph-core-agents (core agents), Phase 12 — learning-agent (learning_agent.py)**

See `../../phases/` (or `../phases/` if this is a top-level folder) for that phase's full brief.
