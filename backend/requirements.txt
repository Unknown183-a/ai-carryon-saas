# Phase 2 — FastAPI Gateway
fastapi
uvicorn[standard]
pydantic

# Phase 2 — Firebase Auth + Firestore
firebase-admin
google-cloud-firestore

# Phase 3 — Redis (Upstash REST client) + rate limiter identity decoding
httpx
pyjwt

# Phase 4 — LangGraph agents, LLM providers, trends, web search
langgraph
google-genai
groq
pytrends

# Phase 6 — multi-tenancy: provider-key encryption at rest (Ch.12d)
cryptography

# Phase 7 — async workers (Ch.15): task queue, thumbnail rendering,
# video rendering (shells out to the real ffmpeg binary — not a pip
# package, see docker/Dockerfile), YouTube upload
celery[redis]
Pillow
google-api-python-client
google-auth
google-auth-oauthlib

# Shared
python-dotenv
requests
google-cloud-secret-manager
google-cloud-run
