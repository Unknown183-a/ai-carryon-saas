Owned by: **Phase 6 — multi-tenancy-channel-factory (POST /workspaces, onboarding flow)**

`onboarding.py`: `get_or_create_workspace()`, the idempotent logic behind `POST /workspaces` (`app/api/routers/workspaces.py`).

See `../../../phases/` for that phase's full brief.
