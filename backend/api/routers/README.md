Owned by: **Phase 2 — fastapi-gateway (channels.py starts here; more routers added in later phases)**

See `../../phases/` (or `../phases/` if this is a top-level folder) for that phase's full brief.
