Owned by: **Prompt templates / prompt library overrides referenced from Phase 4 onward and formalized in Phase 6 (per-channel prompt overrides in the Channel Brain).**

See `../../../phases/` for that phase's full brief.
