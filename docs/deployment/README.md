Deployment runbooks and environment notes that don't belong in code — e.g. how to rotate a
provider key in production, how to roll back a bad deploy, Cloud Run / Railway environment
differences. Starts filling in from Phase 9 (Deployment) onward.
