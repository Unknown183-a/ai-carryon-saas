Generated or hand-written API reference (OpenAPI export, endpoint docs) — starts filling in
from Phase 2 onward as real routers exist. Not required reading to build; useful once the API
surface is big enough that the SAD's prose isn't enough.
