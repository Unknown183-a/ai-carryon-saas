Source files for diagrams referenced in the SAD (e.g. `.excalidraw`, `.drawio`, mermaid `.mmd`
files) — keep the editable source here even though the rendered version lives inside
`../architecture/AI-CarryON-Architecture.html`. Optional; only needed once a diagram needs
updating and the original source matters.
