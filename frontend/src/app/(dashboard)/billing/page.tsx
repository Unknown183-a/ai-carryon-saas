import ComingSoon from "@/components/ComingSoon";

export default function BillingPage() {
  return (
    <ComingSoon
      title="Billing"
      reason="No billing provider is integrated in the backend yet — this is a placeholder in the nav (Ch.12c) until a Stripe (or similar) integration and its endpoints exist."
    />
  );
}
