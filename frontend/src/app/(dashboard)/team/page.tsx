import ComingSoon from "@/components/ComingSoon";

export default function TeamPage() {
  return (
    <ComingSoon
      title="Team"
      reason="Ch.12e's Permission Check currently resolves a single owner_uid per workspace — there's no invite flow or multi-member membership model on the backend yet, so there's nothing for this screen to manage."
    />
  );
}
